using UnityEngine;

namespace Vane {
    /// <summary>
    /// Attach this Component to any GameObject which has a Transform Rotation which represents a direction.
    /// Recommendation: Set Gizmo 3D Icons size to zero and use the Rotate Tool for best use.
    /// </summary>
    public class Vane : MonoBehaviour {
    }
}